{
  "name": "ballerina-mcp",
  "version": "0.1.0",
  "description": "MCP server for the Ballerina plugin",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild server.js --bundle --platform=node --target=node18 --outfile=dist/server.js --legal-comments=eof --minify-whitespace"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "3.24.2"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
